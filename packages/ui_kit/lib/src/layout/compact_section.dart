import 'package:design_system/design_system.dart';

class CompactSection extends SectionLayout {
  const CompactSection({
    super.key,
    required super.title,
    required super.child,
    super.subtitle,
    super.trailing,
    super.inset,
  });
}
