name: timer_engine
description: Reusable timer lifecycle and session engine for timer-based apps.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.7.0

resolution: workspace

dependencies:
  riverpod: ^3.2.1

dev_dependencies:
  test: ^1.25.8
