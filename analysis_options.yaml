include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - '**/*.g.dart'
    - '**/*.freezed.dart'

linter:
  rules:
    - avoid_dynamic_calls
    - avoid_unused_parameters
    - avoid_print
    - prefer_final_locals
