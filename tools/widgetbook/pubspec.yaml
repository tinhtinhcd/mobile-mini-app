name: factory_widgetbook
description: Shared component preview tool for Mobile App Factory.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.7.0
  flutter: '>=3.29.0'

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  app_core: ^0.1.0
  ui_kit: ^0.1.0
  widgetbook: ^3.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
