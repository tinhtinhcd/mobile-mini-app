name: storage
description: Shared local persistence helpers for Mobile App Factory apps.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.7.0
  flutter: '>=3.29.0'

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3
  timer_engine: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
